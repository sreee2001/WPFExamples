<UserControl x:Class="BasicControls.Common.ControlIntroductionSampleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BasicControls.Common"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Feature.Infrastructure.Styles;component/CommonStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Border Style="{StaticResource RoundedPanelBorderStyle}">
        <StackPanel Orientation="Vertical">
            <Label Content="{Binding SamplesIntroductionHeader}" FontSize="16" HorizontalAlignment="Center">
                <Label.ContentTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" FontSize="16" FontWeight="Bold"/>
                    </DataTemplate>
                </Label.ContentTemplate>
            </Label>
            <ContentControl Content="{Binding SampleContent}"/>
        </StackPanel>
    </Border>
</UserControl>
