<UserControl x:Class="BasicControls.Common.ControlIntroductionDescriptionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BasicControls.Common"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Feature.Infrastructure.Styles;component/CommonStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Border Style="{StaticResource RoundedPanelBorderStyle}">
        <StackPanel>
            <TextBlock
                    TextWrapping="Wrap"
                    FontSize="18"
                    FontWeight="Bold"
                    FontFamily="Segoe UI"
                    Foreground="#2E3B4E"
                    TextAlignment="Center"
                    Margin="0 0 0 10"
                    >
                    <Run Text="{Binding Header, Mode=OneWay}" FontWeight="Bold" FontSize="20" />
            </TextBlock>
            <ScrollViewer Height="180">
                <ItemsControl Margin="5" ItemsSource="{Binding DescriptionCollection}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <BulletDecorator Margin="0,5,0,5">
                                <BulletDecorator.Bullet>
                                    <Ellipse Fill="#4682B4" Width="8" Height="8" VerticalAlignment="Center"/>
                                </BulletDecorator.Bullet>
                                <TextBlock
                                        Text="{Binding}"
                                        TextWrapping="Wrap"
                                        FontSize="14"
                                        FontWeight="Normal"
                                        FontFamily="Segoe UI"
                                        Foreground="#2E3B4E"
                                        TextAlignment="Left"
                                        Margin="10 0 10 0">
                                </TextBlock>
                            </BulletDecorator>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </StackPanel>
    </Border>
</UserControl>
