<Window x:Class="BasicControls.Views.LabelExampleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BasicControls.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <TextBlock Text="Different Label Styles in WPF" FontSize="18" FontWeight="Bold" Margin="0,0,0,10" HorizontalAlignment="Center"/>
        <ItemsControl ItemsSource="{Binding LabelSamples}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical" >
                        <WrapPanel Orientation="Horizontal" Margin="10, 10, 10, 10">
                            <Label Content="Content :"/>
                            <TextBox Text="{Binding Content}" Width="150" Height="20"/>
                            <Label Content="FontFamily :"/>
                            <TextBox Text="{Binding FontFamily}" Width="150" Height="20"/>
                            <Label Content="FontSize :"/>
                            <TextBox Text="{Binding FontSize}" Width="30" Height="20"/>
                            <Label Content="FontWeight :"/>
                            <TextBox Text="{Binding FontWeight}" Width="100" Height="20"/>
                            <Label Content="Foreground :"/>
                            <TextBox Text="{Binding Foreground}" Width="100" Height="20"/>
                            <Label Content="Background :"/>
                            <TextBox Text="{Binding Background}" Width="100" Height="20"/>
                            <Label Content="HorizontalAlignment :"/>
                            <TextBox Text="{Binding HorizontalAlignment}" Width="100" Height="20"/>
                            <Label Content="VerticalAlignment :"/>
                            <TextBox Text="{Binding VerticalAlignment}" Width="100" Height="20"/>
                            <Label Content="Width :"/>
                            <TextBox Text="{Binding Width}" Width="40" Height="20"/>
                            <Label Content="Height :"/>
                            <TextBox Text="{Binding Height}" Width="40" Height="20"/>
                        </WrapPanel>
                        <Separator Background="#FFD3D3D3" Height="1" Margin="0,2,0,2" />
                        <Label Content="{Binding Content}" 
                               FontFamily="{Binding FontFamily}"
                               FontSize="{Binding FontSize}"
                               FontWeight="{Binding FontWeight}"
                               Foreground="{Binding Foreground}"
                               Background="{Binding Background}"                               
                               HorizontalAlignment="{Binding HorizontalAlignment}"
                               VerticalAlignment="{Binding VerticalAlignment}"
                               Width="{Binding Width}"
                               Height="{Binding Height}"
                               />
                        <Separator Background="#FFD3D3D3" Height="1" Margin="0,2,0,2" />
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
            
        </ItemsControl>
    </StackPanel>
</Window>
