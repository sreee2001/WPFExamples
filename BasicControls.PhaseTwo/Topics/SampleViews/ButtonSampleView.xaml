<UserControl x:Class="BasicControls.PhaseTwo.Topics.SampleViews.ButtonSampleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:common="clr-namespace:BasicControls.Common;assembly=BasicControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Feature.Infrastructure.Styles;component/CommonStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <StackPanel>
        <Grid >
            <Grid.Resources>
                <Style TargetType="{x:Type Button}">
                    <Setter Property="Margin" Value="30"/>
                    <Setter Property="HorizontalAlignment" Value="Center" />
                    <Setter Property="VerticalAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="10 5 10 5" />
                </Style>
                <Style TargetType="{x:Type ToggleButton}">
                    <Setter Property="Margin" Value="30"/>
                    <Setter Property="HorizontalAlignment" Value="Center" />
                    <Setter Property="VerticalAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="10 5 10 5" />
                </Style>


            </Grid.Resources>
        <!--introductionView_GridContent-->
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            
            <Button Grid.Row="0" Grid.Column="0">
                <Button.Content>
                    Simple Buton
                </Button.Content>
            </Button>
            
            <Button Grid.Row="0" Grid.Column="1">
                <Button.Content>
                    <StackPanel Orientation="Horizontal">
                        <Image Source="../../Images/check_icon_green.png" Width="20" Height="20" Margin="0,0,5,0"/>
                        <TextBlock Text="Button with Image and Text" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button.Content>
            </Button>

            <ToggleButton Grid.Row="1" Grid.Column="0">
                <ToggleButton.Content>
                    Toggle Button Style
                </ToggleButton.Content>
            </ToggleButton>

            <Button Grid.Row="1" Grid.Column="1" 
                    Width="250"
                    Height="40"
                    ToolTip="This is a styled button">
                <TextBlock Text="Styled Button using TextBox"/>
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="LightBlue"/>
                        <Setter Property="Foreground" Value="DarkBlue"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="BorderThickness" Value="2"/>
                        <Setter Property="BorderBrush" Value="Navy"/>
                        <Setter Property="Padding" Value="10,5"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </StackPanel>
</UserControl>
